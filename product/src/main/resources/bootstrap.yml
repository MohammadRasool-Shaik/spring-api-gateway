spring:
    application:
        name: product-service
    cloud:
        config:
            discovery:
                service-id: config-server
                enabled: true
            fail-fast: true
#    profiles:
#        active: dev #If property exists in this profile/file it takes otherwise it checks in default profile set

eureka:
    client:
        serviceUrl:
            defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761/eureka/}